class Program{

  DateTime startTime;
  num duration;

  String title;
  String description;
  String imageUrl;
  String externalUrl;

  Program(this.startTime, this.duration, this.title, this.description, this.imageUrl, this.externalUrl);
}