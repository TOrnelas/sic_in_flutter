class Program{

  num id;
  num startTime;
  num endTime;

  String title;
  String description;
  String imageUrl;

  Program(this.id, this.startTime, this.endTime, this.title, this.description, this.imageUrl);
}